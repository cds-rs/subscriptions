[package]
name = "scenarios"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }

[lints]
workspace = true

[dependencies]
subscriptions = { package = "subscriptions-program", path = "../program", features = ["no-entrypoint"] }
# Local fingerprint checkout (content-superset of turbin3, has the engine
# adapters + IDL extractors + fingerprint machinery; not pushed to cds-rs).
testsvm = { path = "/Users/amal/oss/upstream/anchor-litesvm/crates/testsvm" }
solana-account = "~3.4"
solana-clock = "^3"
solana-instruction = "^3"
solana-keypair = "3.1"
solana-message = "^3.1"
solana-program-pack = "^3"
solana-pubkey = "^4"
solana-rent = "^3"
solana-signer = "^3"
spl-associated-token-account-interface = "2.0.0"
spl-token-2022-interface = "3.0.0"
spl-token-interface = "3.0.0"
serde_json = { workspace = true }
